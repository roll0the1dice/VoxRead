---

# number-sets are a little messy in that the base was converted to a number-set, so we have to match that (simple) case last
- name: pos-neg-number-sets
  tag: number-sets
  match: "count(*)=2 and *[2][.='+' or .='-']"
  replace:
  - test:
      if: "$Verbosity!='Terse'"
      then:
      - T: "a"
  - bookmark: "*[2]/@id"
  - test:
    - if: "*[2][.='+']"
      then: [T: "positivo"]
      else: [T: "negativo"]
  - bookmark: "*[1]/@id"
  - test:
    - if: "*[1][.='ℂ']"
      then: [T: números complexos]
    - else_if: "*[1][.='ℕ']"
      then: [T: números naturais]
    - else_if: "*[1][.='ℚ']"
      then: [T: números racionais]
    - else_if: "*[1][.='ℝ']"
      then: [T: números reais]
    - else_if: "*[1][.='ℤ']"
      then: [T: inteiros]
      else: [x: "*[1][text()]"] # shouldn't happen

- name: dimension-number-sets

  # should be single digit integer at this point (e.g, R^3)
  tag: number-sets
  match: "count(*)=2"
  replace:
  - bookmark: "*[1]/@id"
  - test:
    - if: "*[1][.='ℂ']"
      then: [T: "C"]
    - else_if: "*[1][.='ℕ']"
      then: [T: "N"]
    - else_if: "*[1][.='ℚ']"
      then: [T: "Q"]
    - else_if: "*[1][.='ℝ']"
      then: [T: "R"]
    - else_if: "*[1][.='ℤ']"
      then: [T: "Z"]
      else: [x: "*[1][text()]"] # shouldn't happen
  - bookmark: "*[2]/@id"
  - x: "*[2]"

- name: simple-number-sets
  tag: number-sets
  match: "count(*)=0"
  replace:
  - bookmark: "@id"
  - test:
    - if: ".='ℂ'"
      then: [T: números complexos]
    - else_if: ".='ℕ'"
      then: [T: números naturais]
    - else_if: ".='ℚ'"
      then: [T: números racionais]
    - else_if: ".='ℝ'"
      then: [T: números reais]
    - else_if: ".='ℤ'"
      then: [T: inteiros]
      else: [x: "text()"] # shouldn't happen

- name: real-part
  tag: real-part
  match: "."
  replace:
  - bookmark: "@id"
  - T: a parte real
- name: imaginary-part
  tag: imaginary-part
  match: "."
  replace:
  - bookmark: "@id"
  - T: a parte imaginária# rules on scripted vertical bars ('evaluated at')
- name: evaluated-at-2
  tag: evaluate
  match: "count(*)=2"
  replace:
  - x: "*[1]"
  - pause: auto
  - T: avaliado em
  - pause: auto
  - x: "*[2]"

- name: evaluated-at-3
  tag: evaluate
  match: "count(*)=3"
  replace:
  - x: "*[1]"
  - pause: auto
  - T: avaliado em
  - pause: auto
  - x: "*[3]"
  - T: "avaliado na mesma expressão"
  - x: "*[2]"

- name: permutation
  tag: pochhammer
  match: "count(*)=2"
  replace:
  - x: "*[2]"
  - T: permutações de
  - x: "*[1]"

- name: intervals
  tag: [open-interval, open-closed-interval, closed-interval, closed-open-interval]
  match: "count(*)=2"
  replace:
  - test:
      if: "$Verbosity!='Terse'"
      then:
      - T: "o"
  - test:
    - if: "self::m:open-interval"
      then: [T: "intervalo aberto"]
    - else_if: "self::m:closed-interval"
      then: [T: "intervalo fechado"]
    - else_if: "self::m:open-closed-interval"
      then: [T: "intervalo aberto fechado"]
      else: [T: "intervalo fechado aberto"]
  - test:
      if: "$Verbosity!='Terse'"
      then:
      - T: "de"
      - x: "*[1]"
      - T: "a"
      - x: "*[2]"
      else:
      - x: "*[1]"
      - T: "vírgula"
      - x: "*[2]"

- name: default-point
  tag: point
  match: "count(*)=2"
  replace:
  - test:
      if: "$Verbosity!='Terse'"
      then:
      - T: "o"
  - T: "ponto"
  - x: "*[1]"
  - T: "vírgula"
  - x: "*[2]"

- name: bigop-both
  tag: large-op
  match: "count(*) = 3"
  replace:
  - test:
      if: "$Verbosity!='Terse'"
      then_test:
        # "a integral", "o somatório": o artigo concorda com o operador.
        if: "*[1][self::m:mo][contains('∫∬∭∮∯∰∱∲∳⨋⨌⨍⨎⨏⨐⨑⨒⨓⨔⨕⨖⨗⨘⨙⨚⨛⨜', .)]"
        then: [T: a]
        else: [T: o]
  - x: "*[1]"
  - T: de
  - x: "*[2]"
  - T: a
  - x: "*[3]"
  - test:
      if: "following-sibling::*"
      then: [T: de]

- name: bigop-under
  tag: large-op
  match: "count(*)=2"
  replace:
  - test:
      if: "$Verbosity!='Terse'"
      then_test:
        # "a integral", "o somatório": o artigo concorda com o operador.
        if: "*[1][self::m:mo][contains('∫∬∭∮∯∰∱∲∳⨋⨌⨍⨎⨏⨐⨑⨒⨓⨔⨕⨖⨗⨘⨙⨚⨛⨜', .)]"
        then: [T: a]
        else: [T: o]
  - x: "*[1]"
  - T: sobre
  - x: "*[2]"
  - test:
      if: "following-sibling::*"
      then: [T: de]

- name: largeop
  tag: mrow
  match: "count(*)=2 and IsInDefinition(*[1], 'LargeOperators')"
  replace:
  - test:
      if: "$Verbosity!='Terse'"
      then_test:
        # "a integral", "o somatório": o artigo concorda com o operador.
        if: "*[1][self::m:mo][contains('∫∬∭∮∯∰∱∲∳⨋⨌⨍⨎⨏⨐⨑⨒⨓⨔⨕⨖⨗⨘⨙⨚⨛⨜', .)]"
        then: [T: a]
        else: [T: o]
  - x: "*[1]"
  - T: de
  - x: "*[2]"

- name: msubsup-skip-super
  # handles single, double, etc., prime
  tag: [skip-super, say-super]
  match: "count(*)=3"
  replace:
  - x: "*[1]"
  # CORREÇÃO: a distinção Verbose/conciso do inglês ("subscript"/"sub")
  # tinha colapsado, os dois ramos diziam a mesma coisa. Restaurada,
  # coerente com as abreviações de default.yaml ('subscrito'/'sub').
  # Também restaurada a condição $Impairment='Blindness' do match interno,
  # presente no inglês e perdida na tradução.
  - test:
      if: "$Verbosity='Verbose'"
      then: [T: "subscrito"]
      else: [T: "sub"]
  - x: "*[2]"
  - test:
      if: "not(IsNode(*[2],'leaf') and $Impairment = 'Blindness')"
      then:
      - test:
          if: "$Verbosity='Verbose'"
          then: [T: "fim do subscrito"]
          else: [T: "fim do sub"]
      - pause: short
      else_test:
          if: "*[2][self::m:mi]"   # need a pause in "x sub k prime" so the prime is not associated with the 'k'
          then: [pause: short]
  - test:
      if: "name(.)='say-super'"
      then_test:
        if: "$Verbosity='Verbose'"
        then: [T: "sobrescrito"]
        else: [T: "super"]
  - x: "*[3]"
  - pause: short

# the order of matching is
# 1. does it match the base of an SI unit
# 2. does it match an English unit (if in an English language)
# 3. does it match an SI prefix followed by an SI that accepts SI prefixes
# Due to this order, some things like "ft" and "cd" mean "feet" vs "femto-tonnes" and "pints" vs "pico-tonnes"
# ACRESCENTADO (en): no modo conciso, unidade de uma letra é soletrada
# ("3 m" -> "3 m" soletrado), em vez de expandida para "metros".
- name: unit-terse
  tag: unit
  match: "$Verbosity = 'Terse' and string-length(.)=1"
  replace:
  - bookmark: "@id"
  - spell: "text()"

- name: unit
  tag: unit
  match: "."
  variables:
    # If the coefficient is singular, we don't add the plural ending. Finding the coefficient is tricky
    # Normal case (A) "3m" (parents is mrow), but could also be (B) "3 m^2" (etc.). (parent is msup/mrow)
    # But it might be in a fraction as (C) "3 m/s" (parent is mfrac/mrow) or (D) "3 m^2/s^2" (parent is msup/mfrac/mrow)
    #   or even (E) {3 m^2}/s (parent is msup/mrow)
    # If in a fraction, only look in the numerator to find the coefficient
  - MRowForCoefficient: "IfThenElse(parent::m:mrow, parent::m:mrow,
                         IfThenElse(parent::m:power, ancestor::*[2][self::m:mrow],
                         IfThenElse(parent::m:fraction and not(preceding-sibling::*), ancestor::*[2][self::m:mrow],
                         IfThenElse(parent::m:power[parent::m:fraction and not(preceding-sibling::*)], ancestor::*[3][self::m:mrow], false()) ) ) )"
  - IsSingular: "(not($MRowForCoefficient) and parent::*[name(.)!='skip-super' or *[1][.=1]]) or
                 ($MRowForCoefficient and $MRowForCoefficient[(count(*) = 3 and *[1][self::m:mn and .=1] and *[2]='\u2062')])"
  - Prefix: "''"
  - Word: "''"  
  replace:
  - bookmark: "@id"
  - test:
    # is the whole string match a SI Unit without a prefix?
    - if: "DefinitionValue(., 'Speech', 'SIUnits') != ''"
      then:
      - set_variables: [Word: "DefinitionValue(., 'Speech', 'SIUnits')"]
    - else_if: "DefinitionValue(., 'Speech', 'UnitsWithoutPrefixes') != ''"
      then:
      - set_variables: [Word: "DefinitionValue(., 'Speech', 'UnitsWithoutPrefixes')"]
    - else_if: "DefinitionValue(., 'Speech', 'EnglishUnits') != ''"
      then:
      - set_variables: [Word: "DefinitionValue(., 'Speech', 'EnglishUnits')"]

    # do the first two chars match "da" and the remainder match an SIUnit
    - else_if: "string-length(.) >= 3 and 
                substring(., 1, 2) = 'da' and
                DefinitionValue(substring(., 3), 'Speech', 'SIUnits') != ''"
      then:
      - set_variables:
        - Prefix: "DefinitionValue('da', 'Speech', 'SIPrefixes')"
        - Word: "DefinitionValue(substring(., 3), 'Speech', 'SIUnits')"

    # does the first char match a prefix and the remainder match an SIUnit
    - else_if: "string-length(.) >= 2 and 
                DefinitionValue(substring(., 1, 1), 'Speech', 'SIPrefixes') != ''  and
                DefinitionValue(substring(., 2), 'Speech', 'SIUnits') != ''"
      then:
      - set_variables:
        - Prefix: "DefinitionValue(substring(., 1, 1), 'Speech', 'SIPrefixes')"
        - Word: "DefinitionValue(substring(., 2), 'Speech', 'SIUnits')"

    # not a known unit -- just speak the text, possibly as a plural
    - else:
      - set_variables:
        - Word: "text()"

  - with:
      variables:
      - Word: "IfThenElse(substring($Word, string-length($Word)-2, 3) = 'ián', concat(substring($Word, 1, string-length($Word)-2), ian), $Word)"
      - LastChar: "substring($Word, string-length($Word), 1)"
      - PluralEnding: "IfThenElse($LastChar = 'r' or $LastChar = 'l', 'es',
                      IfThenElse(substring($Word, string-length($Word)-2, 3) = 'ian', 'es', 's' )) "
      replace:
      # somewhat complicated logic to avoid spaces around "-" as in "centi-grams" vs "centi - grams" -- probably doesn't matter
      - test:
          if: "$Prefix = ''"
          then:
          - test:
            - if: "$IsSingular"
              # HACK: '\uF8FE' is used internally for the concatenation char by 'ct' -- this gets the prefix concatinated to the base
              then: [x: "$Word"]
            - else_if: "DefinitionValue($Word, 'Speech', 'PluralForms') != ''"
              then: [x: "DefinitionValue($Word, 'Speech', 'PluralForms')"]
              else: [x: "concat($Word, $PluralEnding)"]
          else:
          - x: "$Prefix"
          - test:
            - if: "$IsSingular"
              # HACK: '\uF8FE' is used internally for the concatenation char by 'ct' -- this gets the prefix concatinated to the base
              then: [x: "concat('\uF8FE', $Word)"]
            - else_if: "DefinitionValue($Word, 'Speech', 'PluralForms') != ''"
              then: [x: "concat('\uF8FE', DefinitionValue($Word, 'Speech', 'PluralForms'))"]
              else: [x: "concat('\uF8FE', $Word, $PluralEnding)"]



# ACRESCENTADO: valores monetários "R$ 100" -> "100 reais".
# Depende de CurrencySymbols e dos plurais em definitions.yaml.
- name: currency
  tag: mrow
  match: "count(*)=3 and DefinitionValue(*[1], 'Speech', 'CurrencySymbols') != ''"
  variables:
    # se o valor é 1, não pluraliza
  - IsSingular: "*[3][self::m:mn and .=1] and *[2]='\u2062'"
  - CurrencyWord: "DefinitionValue(*[1], 'Speech', 'CurrencySymbols')"
  replace:
  - bookmark: "*[3]/@id"
  - x: "*[3]"
  - test:
    - if: "$IsSingular"
      then: [x: "$CurrencyWord"]
    - else_if: "DefinitionValue($CurrencyWord, 'Speech', 'PluralForms') != ''"
      then: [x: "DefinitionValue($CurrencyWord, 'Speech', 'PluralForms')"]
      else: [x: "DefinitionValue(*[1], 'Speech', 'CurrencySymbols')", ct: "s"]

- name: sin
  tag: mi
  match: ".='sin' or .='sen'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      then: [T: sen]
      else: [T: seno]
- name: cos
  tag: mi
  match: ".='cos'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      then: [T: cos]
      else: [T: cosseno]
- name: tan
  tag: mi
  match: ".='tan' or .='tg'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      then: [T: tan]
      else: [T: tangente]
- name: sec
  tag: mi
  match: ".='sec'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      then: [T: sec]
      else: [T: secante]
- name: csc
  tag: mi
  match: ".='csc'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      # CORREÇÃO: dizia "sec", idêntico à regra sec; cossecante e secante
      # ficavam indistinguíveis na fala concisa.
      then: [T: cossec]
      else: [T: cossecante]
- name: cot
  tag: mi
  match: ".='cot'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      then: [T: cotan]
      else: [T: cotangente]

- name: sinh
  tag: mi
  match: ".='sinh'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      then: [T: seno hiperbólico]
      else: [T: seno hiperbólico]
- name: cosh
  tag: mi
  match: ".='cosh'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      then: [T: cosseno hiperbólico]
      else: [T: cosseno hiperbólico]
- name: tanh
  tag: mi
  match: ".='tanh'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      then: [T: tangente hiperbólica]
      else: [T: tangente hiperbólica]
- name: sech
  tag: mi
  match: ".='sech'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      then: [T: secante hiperbólica]
      else: [T: secante hiperbólica]
- name: csch
  tag: mi
  match: ".='csch'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      then: [T: cossecante hiperbólica]
      else: [T: cossecante hiperbólica]
- name: coth
  tag: mi
  match: ".='coth'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"

      then: [T: cotangente hiperbólica]
      else: [T: cotangente hiperbólica]

- name: exponential
  tag: mi
  match: ".='exp'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      then: [T: "exp"]
      else: [T: "exponencial"]
- name: covariance
  tag: mi
  match: ".='Cov'"
  replace:
  - bookmark: "@id"
  - test:
      if: "$Verbosity='Terse'"
      then: [T: "Cov"]
      else: [T: "covariância"]

-
  # CORREÇÃO: faltava o 'Log' maiúsculo (valor principal), presente no inglês.
  # Sem ele, "Log x" caía na regra genérica de mi e saía "Log de x", perdendo a
  # distinção. A tradução de "principal value log" ainda não foi verificada por
  # falante nativo -- daí o 't:' minúsculo.
  name: log    # handle log, principal Log, and ln (if in an mrow, 'intents' are used)
  tag: mi
  match: ".='log' or .='Log' or .='ln'"
  replace:
  - bookmark: "@id"
  - test:
    - if: "$Verbosity!='Terse'"
      then: [T: "o"]      # phrase('the' logarithm function is used in mathematics)
  - test:
    - if: ".= 'log'"
      then: [T: "log"]      # phrase(the 'log' function is used in mathematics)
    - else_if: ".= 'Log'"
      then: [t: "log do valor principal"]      # (en: 'principal value log')
    - else_if: "$Verbosity='Terse'"
      then: [SPELL: "'ln'"]
      else: [T: "logaritmo natural"]      # phrase(the 'natural log' function is used in mathematics)

- name: multi-line
  # audit-ignore: divergência deliberada do en — plural por cadeias, não por concatenação de s
  #   that eliminates the need for the if: else_if: ...
  # IDEA:  set a variable with the word to say for the row (e.g., RowLabel = Row/Case/Line/...)
  tag: [piecewise, system-of-equations, lines]
  match: "."
  variables: [IsColumnSilent: true()]
  replace:
  - x: "count(*)"
  # CORREÇÃO: o plural era feito concatenando "s" (ct: "s"), o que produzia
  # "equaçãos". Substituído por duas cadeias, singular e plural, escolhidas
  # pela contagem — mesma correção já aplicada no ClearSpeak.
  - test:
      if: "count(*) > 1"
      then:
      - test:
        - if: "self::m:piecewise"
          then: [T: "casos"]
        - else_if: "self::m:system-of-equations"
          then: [T: "equações"]
          else: [T: "linhas"]
      else:
      - test:
        - if: "self::m:piecewise"
          then: [T: "caso"]
        - else_if: "self::m:system-of-equations"
          then: [T: "equação"]
          else: [T: "linha"]
  - pause: short
  - x: "*"

- name: default-multiline
  tag: [mtr, mlabeledtr]
  match: "parent::m:piecewise or parent::m:system-of-equations or parent::m:lines"
  replace:
  - pause: medium
  - test:
    - if: "parent::m:piecewise"
      then: [T: "caso"]
    - else_if: "parent::m:system-of-equations"
      then: [T: "equação"]
      else: [T: "linha"]
  - x: "count(preceding-sibling::*)+1"
  - test:
      if: .[self::m:mlabeledtr]
      then:
      - T: rotulada com
      - x: "*[1]/*"
  - pause: medium
  - test:
      if: .[self::m:mlabeledtr]
      then: [x: "*[position()>1]"]
      else: [x: "*"]

- name: default-multiline
  tag: mtd
  match: "parent::*[parent::m:piecewise or parent::m:system-of-equations or parent::m:lines]"
  variables: [LongPause: "$SpeechStyle = 'ClearSpeak' and $ClearSpeak_MultiLinePausesBetweenColumns = 'Long'"]
  replace:
  - x: "*"
  - test:
    # short pause after each element; medium pause if last element in a row; long pause for last element in matrix unless ClearSpeak override
    - if: "count(following-sibling::*) > 0"
      then_test:
        if: "$LongPause"
        then: [pause: medium]
        else: [pause: short]
    - else_if: "count(../following-sibling::*) > 0"
      then_test:
        if: "$LongPause"
        then: [pause: long]
        else: [pause: medium]
      else: [pause: long]

# Matrix/Determinant rules
# matrix and determinant are the same other than "matrix"/"determinant" based on the bracketing chars
# the pausing logic is pushed down to the <mtd>
# the rules either speak the <mtr>s (to get "row n") or the <mtd>s. "column n" spoken if $IsColumnSilent is false
# ACRESCENTADO: regra presente em en/ e ausente em es/ (e portanto em pt).
# Sem ela, dízimas periódicas (0,333...) não eram anunciadas como periódicas.
- name: repeating-decimal
  tag: repeating-decimal
  match: "."
  replace:
  - x: "*[1]"
  - T: "com dígitos periódicos"
  - spell: "*[2]"

# NOTA: o inglês funde matrix e determinant numa regra só. Em português os
# artigos diferem ("a matriz" / "o determinante"), então a separação é
# deliberada, não é lacuna de paridade.
- name: 1x1-matrix
  # audit-ignore: divergência deliberada do en — separada de 1x1-determinant: artigos de gêneros diferentes
  tag: matrix
  variables: [IsColumnSilent: true()]
  match: "count(*)=1 and *[self::m:mtr][count(*) = 1]"
  replace:
  - OT: "a"
  - T: "matriz 1 por 1"
  - T: "com o elemento"
  - x: "*[1]/*"

- name: 1x1-determinant
  # audit-ignore: divergência deliberada do en — idem
  tag: determinant
  variables: [IsColumnSilent: true()]
  match: "count(*)=1 and *[self::m:mtr][count(*) = 1]"
  replace:
  - OT: "o"
  - T: "determinante 1 por 1"
  - T: "com o elemento"
  - x: "*[1]/*"


- name: zero-matrix
  tag: matrix
  match: "not( */*/*[not(self::m:mn and .= 0)] )"
  replace:
  - T: "a matriz nula"
  - x: count(*)
  - T: "por"
  - x: count(*[self::m:mtr][1]/*)
  - pause: long

- name: identity-matrix
  tag: matrix
  match:
  - "count(*) = count(*[1]/*) and "
  - "not( */*[count(preceding-sibling::*) = count(../preceding-sibling::*)]/*[not(self::m:mn and .= 1)] ) and "
  - "not( */*[count(preceding-sibling::*) != count(../preceding-sibling::*)]/*[not(self::m:mn and .= 0)] )"
  replace:
  - T: "a matriz identidade"
  - x: count(*)
  - T: "por"
  - x: count(*[self::m:mtr][1]/*)
  - pause: long

- name: diagonal-matrix
  tag: matrix
  match:
  - "count(*) = count(*[1]/*) and "
  - "not( */*/*[not(self::m:mn and .= 0)]"
  - "          [count(../preceding-sibling::*)!=count(../../preceding-sibling::*)]"
  - "    )"
  replace:
  - T: "a matriz diagonal"
  - x: count(*)
  - T: "por"
  - x: count(*[self::m:mtr][1]/*)
  - pause: long
  - insert:
      nodes: "(*/*/*[not(self::m:mn and .= 0)]/..)"
      replace: [pause: auto]
  - pause: long

# simpler reading methods for smaller matrices if the entries are simple
- name: 2-or-3x1-matrix
  tag: matrix
  variables: [IsColumnSilent: true()]
  match:
  - "$ClearSpeak_Matrix != 'SpeakColNum' and " # "simple" isn't used for this preference
  - "*[self::m:mtr][count(*) = 1] and " # one column
  - count(*)<=3 and # at least two rows
  - IsNode(*/*/*,'simple') # IsNode() returns true if all the nodes are simple
  replace:
  - test:
      if: "$ClearSpeak_Matrix = 'Vector' or $ClearSpeak_Matrix = 'EndVector'"
      then: [T: "o vetor coluna"]
      else: [T: "a matriz coluna"]
  - x: count(*)
  - T: "por 1 coluna"
  - pause: long
  - x: "*/*"
  - test:
      if: "$ClearSpeak_Matrix = 'EndMatrix' or $ClearSpeak_Matrix = 'EndVector'"
      then:
      - T: "fim"
      - test:
          if: $ClearSpeak_Matrix = 'EndVector'
          then: [T: vetor]
          else: [T: matriz]

- name: default-column-matrix
  tag: matrix
  variables: [IsColumnSilent: true()]
  match: "*[self::m:mtr][count(*) = 1]"
  replace:
  - test:
      if: "$ClearSpeak_Matrix = 'Vector' or $ClearSpeak_Matrix = 'EndVector'"
      then: [T: "o vetor"]
      else: [T: "a matriz"]
  - x: "count(*)"
  - T: "por 1 coluna"
  - pause: long
  - x: "*" # select the rows (mtr)
  - test:
      if: "$ClearSpeak_Matrix = 'EndMatrix' or $ClearSpeak_Matrix = 'EndVector'"
      then: [T: "fim da matriz"]

- name: 1x2-or-3-matrix
  tag: matrix
  variables: [IsColumnSilent: "$SpeechStyle = 'ClearSpeak' and $ClearSpeak_Matrix != 'SpeakColNum'"]
  match:
  - "$ClearSpeak_Matrix != 'SpeakColNum' and " # "simple" isn't used for this preference
  - count(*)=1  and # one row
  - count(*[1]/*)<=3 and # at least two cols
  - IsNode(*/*/*,'simple') # IsNode() returns true if all the nodes are simple
  replace:
  - test:
      if: "$ClearSpeak_Matrix = 'Vector' or $ClearSpeak_Matrix = 'EndVector'"
      then: [T: "o vetor"]
      else: [T: "a matriz"]
  - T: "1 por"
  - x: count(*/*)
  - T: "linha"
  - pause: long
  - x: "*/*"
  - test:
      if: "$ClearSpeak_Matrix = 'EndMatrix' or $ClearSpeak_Matrix = 'EndVector'"
      then:
      - T: "fim"
      - test:
          if: $ClearSpeak_Matrix = 'EndMatrix'
          then: [T: matriz]
          else: [T: vetor]

- name: default-row-matrix
  tag: matrix
  variables: [IsColumnSilent: "$SpeechStyle = 'ClearSpeak' and $ClearSpeak_Matrix = 'SilentColNum'"]
  match: "count(*)=1" # one row
  replace:
  - test:
      if: "$ClearSpeak_Matrix = 'Vector' or $ClearSpeak_Matrix = 'EndVector'"
      then: [T: "o vetor"]
      else: [T: "a matriz"]
  - T: "1 por"
  - x: "count(*/*)"
  - T: "linha"
  - pause: long
  - x: "*/*" # select the cols (mtd)
  - test:
      if: "$ClearSpeak_Matrix = 'EndMatrix' or $ClearSpeak_Matrix = 'EndVector'"
      then:
      - T: "fim"
      - test:
          if: $ClearSpeak_Matrix = 'EndMatrix'
          then: [T: matriz]
          else: [T: vetor]

- name: simple-small-matrix
  tag: [matrix, determinant]
  match:
  - "$ClearSpeak_Matrix != 'SpeakColNum' and " # "simple" isn't used for this preference
  - (count(*)<=3 and count(*[1]/*)<=3) and # no bigger than a 3x3 matrix
  - IsNode(*/*/*,'simple') # IsNode() returns true if all the nodes are simple
  variables: [IsColumnSilent: "$SpeechStyle = 'ClearSpeak' and $ClearSpeak_Matrix != 'SpeakColNum'"]
  replace:
  - test:
      if: "self::m:determinant"
      then: [T: "o determinante"]
      else: [T: "a matriz"]
  - x: count(*)
  - T: por
  - x: count(*[self::m:mtr][1]/*)
  - pause: long
  - x: "*"
  - test:
      if: "$ClearSpeak_Matrix = 'EndMatrix' or $ClearSpeak_Matrix = 'EndVector'"
      then:
      - T: fim
      - test:
          if: "self::m:determinant"
          then: [T: "determinante"]
          else: [T: "matriz"]

- name: default-matrix
  tag: [matrix, determinant]
  variables: [IsColumnSilent: "$SpeechStyle = 'ClearSpeak' and $ClearSpeak_Matrix = 'SilentColNum'"]
  match: "not(@data-intent-property)"
  replace:
  - test:
      if: "self::m:determinant"
      then: [T: "o determinante"]
      else: [T: "a matriz"]
  - x: "count(*)"
  - T: por
  - x: "count(*[self::m:mtr][1]/*)"
  - pause: long
  - x: "*"
  - test:
      if: "$ClearSpeak_Matrix = 'EndMatrix' or $ClearSpeak_Matrix = 'EndVector'"
      then:
      - T: fim
      - test:
          if: "self::m:determinant"
          then: [T: determinante]
          else: [T: matriz]

- name: chemistry-msub

  tag: [chemical-formula]
  match: "*[1][.='msub']"
  replace:
  - x: "*[2]"
  - test:
      if: "$Verbosity='Verbose'"
      then: [T: "subscrito"]
      else_test:
        if: "$Verbosity='Medium'"
        then: [T: "sub"]
  - x: "*[3]"

# ACRESCENTADO: dimensões de matriz anotadas como dimension-product
# ("3×5" com intent de dimensão) fala "3 por 5".
- name: dimension-by
  tag: mrow
  match: dimension-product
  replace:
  - insert:
      nodes: "*"
      replace: [T: "por", pause: auto]

- name: chemistry-msup
  tag: [chemical-formula]
  match: "count(*)=3 and *[1][.='msup']"
  replace:
  - x: "*[2]"
  - test:
      if: "$Verbosity='Verbose'"
      then: [T: "sobrescrito"]
      else_test:
        if: "$Verbosity='Medium'"
        then: [T: "super"]
  - x: "*[3]"
  - test:
      if: "following-sibling::*[1][.='+' or .='-']" # a little lazy -- assumes chemistry superscripts end with + or -
      then: [pause: medium]

-
  # There currently is no way to do sub/super for n-ary number of args
  # Instead, we just deal with up to two prescripts and up to four postscripts (repeating blocks of similar code [UGLY!])
  # This hopefully covers all reasonable cases...
  name: chemistry-scripts
  tag: [chemical-formula, chemical-nuclide]
  variables:
  # computing the number of postscripts is messy because of <mprescripts> being optionally present -- we use "mod" to get the count right
  - Prescripts: "m:mprescripts/following-sibling::*"
  - NumChildren: "count(*)" # need to stash this since the count is wrong inside '*[...]' below
  - Postscripts: "*[position()>1 and position() < (last() + ($NumChildren mod 2) -count($Prescripts))]"
  match: . # should only be msubsup or mmultiscripts at this point
  replace:
  - test:
      if: "$Prescripts" # we have at least one pre sub/super 
      then:
      # nuclide: speak the superscript first
      - test:
          if: "not($Prescripts[2][self::m:none])"
          then:
          - test:
              if: "$Verbosity='Verbose'"
              then: [T: "sobrescrito"]
              else_test:
                if: "$Verbosity='Medium'"
                then: [T: "super"]
          - x: "$Prescripts[2]"
          - pause: "short"
      - test:
          if: "not($Prescripts[1][self::m:none])"
          then:
          - test:
              if: "$Verbosity='Verbose'"
              then: [T: "subscrito"]
              else_test:
                if: "$Verbosity='Medium'"
                then: [T: "sub"]
          - x: "$Prescripts[1]"
          - pause: "short"
      - test:
          if: "count($Prescripts) > 2" # can this happen for chemistry??? we allow for one *extra* pre sub/super pair
          then:
          - test:
              if: "not($Prescripts[4][self::m:none])"
              then:
              - test:
                  if: "$Verbosity='Verbose'"
                  then: [T: "sobrescrito"]
                  else_test:
                    if: "$Verbosity='Medium'"
                    then: [T: "super"]
              - x: "$Prescripts[4]"
              - pause: "short"
          - test:
              if: "not($Prescripts[3][self::m:none])"
              then:
              - test:
                  if: "$Verbosity='Verbose'"
                  then: [T: "subscrito"]
                  else_test:
                    if: "$Verbosity='Medium'"
                    then: [T: "sub"]
              - x: "$Prescripts[3]"
              - pause: "short"
  - x: "*[1]" # base
  - test:
      if: "$Postscripts"
      then:
      - test:
          if: "not($Postscripts[1][self::m:none])"
          then:
          - test:
              if: "$Verbosity='Verbose'"
              then: [T: "subscrito"]
              else_test:
                if: "$Verbosity='Medium'"
                then: [T: "sub"]
          - x: "$Postscripts[1]"
          - pause: "short"
      - test:
          if: "not($Postscripts[2][self::m:none])"
          then:
          - test:
              if: "$Verbosity='Verbose'"
              then: [T: "sobrescrito"]
              else_test:
                if: "$Verbosity='Medium'"
                then: [T: "super"]
          - x: "$Postscripts[2]"
          - pause: "short"
      - test:
          if: "count($Postscripts) > 2"
          then:
          - test:
              if: "not($Postscripts[3][self::m:none])"
              then:
              - test:
                  if: "$Verbosity='Verbose'"
                  then: [T: "subscrito"]
                  else_test:
                    if: "$Verbosity='Medium'"
                    then: [T: "sub"]
              - x: "$Postscripts[3]"
              - pause: "short"
          - test:
              if: "not($Postscripts[4][self::m:none])"
              then:
              - test:
                  if: "$Verbosity='Verbose'"
                  then: [T: "sobrescrito"]
                  else_test:
                    if: "$Verbosity='Medium'"
                    then: [T: "super"]
              - x: "$Postscripts[4]"
              - pause: "short"
          - test:
              if: "count($Postscripts) > 4"
              then:
              - test:
                  if: "not($Postscripts[5][self::m:none])"
                  then:
                  - test:
                      if: "$Verbosity='Verbose'"
                      then: [T: "subscrito"]
                      else_test:
                        if: "$Verbosity='Medium'"
                        then: [T: "sub"]
                  - x: "$Postscripts[5]"
                  - pause: "short"
              - test:
                  if: "not($Postscripts[6][self::m:none])"
                  then:
                  - test:
                      if: "$Verbosity='Verbose'"
                      then: [T: "sobrescrito"]
                      else_test:
                        if: "$Verbosity='Medium'"
                        then: [T: "super"]
                  - x: "$Postscripts[6]"
                  - pause: "short"
              - test:
                  if: "count($Postscripts) > 6"
                  then:
                  - test:
                      if: "not($Postscripts[7][self::m:none])"
                      then:
                      - test:
                          if: "$Verbosity='Verbose'"
                          then: [T: "subscrito"]
                          else_test:
                            if: "$Verbosity='Medium'"
                            then: [T: "sub"]
                      - x: "$Postscripts[7]"
                      - pause: "short"
                  - test:
                      if: "not($Postscripts[8][self::m:none])"
                      then:
                      - test:
                          if: "$Verbosity='Verbose'"
                          then: [T: "sobrescrito"]
                          else_test:
                            if: "$Verbosity='Medium'"
                            then: [T: "super"]
                      - x: "$Postscripts[8]"
                      - pause: "short"
      - test:
          if: "$Postscripts[last()][not(self::m:none)] and following-sibling::*[1][.='+' or .='-']"
          then: [pause: medium]

- name: chemistry


  tag: chemical-equation
  match: "."
  replace:
  - x: "*"

- name: chemical-element
  tag: chemical-element
  match: "."
  replace:
  - bookmark: "@id"
  - spell: text()
  - pause: short

- name: chemical-state
  tag: chemical-state
  match: "count(*)=1"
  replace:
  - bookmark: "*[1]/@id"
  - test:
    - if: ".='s'"
      then: [T: "sólido"]
    - else_if: ".='l'"
      then: [T: "líquido"]
    - else_if: ".='g'"
      then: [T: "gasoso"]
      else: [T: "aquoso"] # (aq)
  - pause: short

- name: chemical-formula-operator-bond
  tag: chemical-formula-operator
  match: "@data-chemical-bond"
  replace:
  # FIX: this might be better/more efficient if in unicode.yaml
  - bookmark: "@id"
  - test:
    - if: ".='-' or .=':'"
      then: [T: "ligação simples"]
    - else_if: ".='=' or .='∷'"   # U+2237: o canonicalizador converte '::' nesse caractere
      then: [T: "ligação dupla"]
    - else_if: ".='≡'"
      then: [T: "ligação tripla"]
    - else_if: ".='≣'"
      then: [T: "ligação quádrupla"]
      else: [x: "text()"]

- name: chemical-formula-operator
  tag: chemical-formula-operator
  match: "."
  replace:
    x: "text()"

- name: chemical-arrow-operator
  tag: chemical-arrow-operator
  match: "."
  replace:
  # FIX: this might be better/more efficient if in unicode.yaml
  - bookmark: "@id"
  - test:
    - if: ".='→' or .='⟶'"
      then_test:
        if: "$Verbosity='Terse'"
        then: [T: "forma"]
        else: [T: "reage formando"]
    - else_if: ".='⇌' or .='🣑'" # U+01F8D1
      then: [T: "está em equilíbrio com"]
    - else_if: ".='🣓'" # U+1F8D3
      then: [T: "está em equilíbrio deslocado para a esquerda com"]
    - else_if: ".='🣒'" # U+1F8D2
      then: [T: "está em equilíbrio deslocado para a direita com"]
      else: [x: "*"]

- name: chemical-equation-operator
  tag: chemical-equation-operator
  match: "."
  replace:
  - bookmark: "@id"
  - x: "text()"

- name: none
  tag: none
  match: "../../*[self::m:chemical-formula or self::m:chemical-nuclide]"
  replace:
  - t: "" # don't say anything

# ACRESCENTADO: fora da química, <none/> cai na regra 'default-text', que fala o
# nome da tag -- em inglês isso dá "none", que é aceitável lá, mas em português
# saía a palavra inglesa. Aqui traduzimos.
- name: none-default
  tag: none
  match: "."
  replace:
  - T: "nenhum"

- name: ignore-intent-wrapper
  tag: intent-wrapper
  match: "."
  replace:
  - x: "*"
